apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.configuration.name }}
  namespace: {{ .Release.Namespace }}
data:
  {{- range $key, $value := .Values.configuration.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
